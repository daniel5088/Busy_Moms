/**
 * NetworkBanner - shows "No internet connection" banner at top
 * Note: Requires @react-native-community/netinfo to be installed
 * Currently a placeholder implementation
 */

import React from 'react';

export function NetworkBanner() {
  // This will be implemented after installing @react-native-community/netinfo
  // For now, it returns null to avoid compilation errors
  return null;
}
